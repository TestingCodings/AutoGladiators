<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AutoGladiators.Client.Pages.MainMenuPage"
             Title="AutoGladiators"
             BackgroundColor="#0A0A0F">
    
    <ScrollView>
        <Grid RowDefinitions="Auto,*,Auto" Padding="20">
            
            <!-- Header Section -->
            <StackLayout Grid.Row="0" Spacing="10" Margin="0,20,0,40">
                <Label Text="⚔️ AUTOGLADIATORS" 
                       FontSize="32" 
                       FontAttributes="Bold"
                       TextColor="#FFD700"
                       HorizontalTextAlignment="Center"/>
                <Label Text="Battle • Collect • Conquer" 
                       FontSize="16" 
                       TextColor="#B0B0B0"
                       HorizontalTextAlignment="Center"/>
            </StackLayout>
            
            <!-- Main Menu Cards -->
            <StackLayout Grid.Row="1" Spacing="15">
                
                <!-- Adventure Card -->
                <Frame BackgroundColor="#1A1A2E" 
                       CornerRadius="12" 
                       HasShadow="True"
                       Padding="0">
                    <Button Text="🗺️ ADVENTURE" 
                            FontSize="18" 
                            FontAttributes="Bold"
                            TextColor="#00FF88"
                            BackgroundColor="Transparent"
                            HeightRequest="60"
                            Clicked="OnAdventureClicked"/>
                </Frame>
                
                <!-- Bot Roster Card -->
                <Frame BackgroundColor="#1A1A2E" 
                       CornerRadius="12" 
                       HasShadow="True"
                       Padding="0">
                    <Button Text="🤖 BOT ROSTER" 
                            FontSize="18" 
                            FontAttributes="Bold"
                            TextColor="#4A9EFF"
                            BackgroundColor="Transparent"
                            HeightRequest="60"
                            Clicked="OnBotRosterClicked"/>
                </Frame>
                
                <!-- Inventory Card -->
                <Frame BackgroundColor="#1A1A2E" 
                       CornerRadius="12" 
                       HasShadow="True"
                       Padding="0">
                    <Button Text="🎒 INVENTORY" 
                            FontSize="18" 
                            FontAttributes="Bold"
                            TextColor="#FF6B35"
                            BackgroundColor="Transparent"
                            HeightRequest="60"
                            Clicked="OnInventoryClicked"/>
                </Frame>
                
                <!-- Battle Arena Card -->
                <Frame BackgroundColor="#1A1A2E" 
                       CornerRadius="12" 
                       HasShadow="True"
                       Padding="0">
                    <Button Text="⚡ BATTLE ARENA" 
                            FontSize="18" 
                            FontAttributes="Bold"
                            TextColor="#FF4757"
                            BackgroundColor="Transparent"
                            HeightRequest="60"
                            Clicked="OnBattleArenaClicked"/>
                </Frame>
                
            </StackLayout>
            
            <!-- Footer Section -->
            <StackLayout Grid.Row="2" Spacing="10" Margin="0,40,0,20">
                <Button Text="⚙️ Settings" 
                        FontSize="14" 
                        TextColor="#B0B0B0"
                        BackgroundColor="#2A2A3E"
                        CornerRadius="8"
                        HeightRequest="45"
                        Clicked="OnSettingsClicked"/>
                        
                <Button Text="🐛 Debug Menu" 
                        FontSize="14" 
                        TextColor="#B0B0B0"
                        BackgroundColor="#2A2A3E"
                        CornerRadius="8"
                        HeightRequest="45"
                        Clicked="OnDebugMenuClicked"/>
            </StackLayout>
            
        </Grid>
    </ScrollView>
    
</ContentPage>
